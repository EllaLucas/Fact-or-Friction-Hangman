"""Test for my function the controls gameplay, play_hangman."""

from functions import play_hangman 

def test_play_hangman():
    """Tests for the 'play_hangman' function."""
    assert isinstance(fact, string)
    assert process_guess('many oranges are green when they’re ripe', 'many oranges are green when they’re ripe') == 'You got it! Enjoy the knowledge.' )
    assert isinstance(guess, string)
    assert grab_fact() == True
